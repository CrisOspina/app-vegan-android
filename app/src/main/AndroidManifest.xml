<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vegan">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".actividades.ContenedorInstruccionesActivity"
            android:label="@string/title_activity_contenedor_instrucciones"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity android:name=".formularios.FormCompradorActivity" />
        <activity android:name=".formularios.FormComercianteActivity" />
        <activity android:name=".formularios.FormAgricultoresActivity" />
        <activity android:name=".actividades.RegistrarActivity" />
        <activity android:name=".actividades.LoginActivity" />
        <activity android:name=".actividades.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"/>
    </application>

</manifest>